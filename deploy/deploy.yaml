kind: Namespace
apiVersion: v1
metadata:
  name: captain-system

---

# ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: captain-captain-server
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: captain-captain-server
  namespace: captain-system

---

# ServiceAccount
apiVersion: v1
kind: ServiceAccount
metadata:
  name: captain-captain-server
  namespace: captain-system

---

# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: captain-captain-server
  name: captain-captain-server
  namespace: captain-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: captain-captain-server
  template:
    metadata:
      labels:
        app: captain-captain-server
    spec:
      containers:
      - image: DOCKER_REGISTRY/cuboss/captain-server:v0.1.0
        name: captain-captain-server
        ports:
        - containerPort: 9090
        resources:
          limits:
            cpu: 200m
            memory: 200Mi
          requests:
            cpu: 100m
            memory: 100Mi
      serviceAccountName: captain-captain-server

---

# Service
apiVersion: v1
kind: Service
metadata:
  labels:
    app: captain-captain-server
  name: captain-captain-server
  namespace: captain-system
spec:
  ports:
  - name: http
    port: 9090
    targetPort: http
  selector:
    app: captain-captain-server
